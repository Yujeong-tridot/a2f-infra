apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: avadot-ingress
  namespace: avadot-prod
  annotations:
    # ALB 기본 설정
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/target-type: ip
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTP": 80}, {"HTTPS": 443}]'
    # HTTP -> HTTPS 리다이렉트
    alb.ingress.kubernetes.io/ssl-redirect: '443'
    # SSL 인증서
    alb.ingress.kubernetes.io/certificate-arn: arn:aws:acm:ap-northeast-2:247124030538:certificate/52e8e09e-eef6-4487-aeb8-8b12687b3f04
    # WebSocket 및 로드밸런싱 설정
    alb.ingress.kubernetes.io/target-group-attributes: 'deregistration_delay.timeout_seconds=60,stickiness.enabled=true,stickiness.lb_cookie.duration_seconds=86400'
    alb.ingress.kubernetes.io/load-balancer-attributes: 'idle_timeout.timeout_seconds=3600'
spec:
  ingressClassName: alb
  rules:
  # Backend REST API
  - host: api.avadot.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: backend
            port:
              number: 4008
  # Tarot WebSocket
  - host: tarot.avadot.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: tarot
            port:
              number: 4007
  # Blind Date WebSocket
  - host: blinddate.avadot.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: blind-date
            port:
              number: 4010
  # Custom Chat WebSocket
  - host: chat.avadot.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: custom-chat
            port:
              number: 4009
  # Concierge WebSocket
  - host: concierge.avadot.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: concierge
            port:
              number: 4011
