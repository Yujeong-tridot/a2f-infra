apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  type: NodePort
  ports:
  - port: 4008
    targetPort: 4008
    nodePort: 30008  # localhost에서 이 포트로 접근
    protocol: TCP
    name: http
