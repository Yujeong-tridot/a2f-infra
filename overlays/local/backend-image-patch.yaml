apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
spec:
  template:
    spec:
      containers:
      - name: backend
        image: avadot-backend:local
        imagePullPolicy: Never  # 로컬 이미지 사용
